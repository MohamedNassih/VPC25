"""VPC25 scripts package.

Note: we intentionally avoid importing submodules here to keep `python -m scripts.<module>`
clean and to prevent heavy side effects at package import time.
"""
# Do not import submodules at package init.
